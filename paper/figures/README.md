# Manuscript figures

## Figure 1: complete

`figure1_workflow.svg` is the editable vector source. `make_figure1.py` recreates it using the Python standard library. `figure1_workflow.png` is a high-resolution preview, generated with CairoSVG.

```sh
python paper/figures/make_figure1.py --png
```

The six panels distinguish photography/CAMID review, Gallery indexing, uploaded-photo inference, separate paired-collection classification, Atlas data types and independent releases. No specimen photograph, AI-generated interface or quantitative chart is included. The paired benchmark is not attached to upload inference. The diagram and caption were checked together against the Phase 1/2 scope constraints.

## Figure 2: authentic captures required

The final caption is retained in `../manuscript.md`. The four exact captures, rights checks and per-panel provenance fields are specified in `../phase3_author_actions_20260922.md`. A caption-only reading copy is not a completed Figure 2 plate. No missing image path is inserted into the manuscript.

## Setup assets for S1

The textual protocol is complete. Original authorised setup photographs were not available in this checkout. No substitute photograph was generated.
